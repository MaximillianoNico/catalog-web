import "./search-layout.styles.scss";

const Component = ({ children }: { children: React.ReactNode }) => {
  return (
    <div className='layout-discovery'>
      {children}
    </div>
  )
}

export default Component;
